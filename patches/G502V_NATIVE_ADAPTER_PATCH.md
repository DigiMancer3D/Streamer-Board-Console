# G502V Native SBC Adapter Patch

Install bridge/support marker:

```bash
./tools/patch_g502v_adapter.py --app-dir "$HOME/g502 vis" --apply
```

Full live listener hook remains staged for the next automated patch.
