# Soundcard Native SBC Adapter Patch

Try:

```bash
./tools/patch_soundcard_adapter.py --app-dir "$HOME/sound card" --apply
```

The patcher creates a backup and installs `sbc_app_bridge.py`.
After patching, restart Soundcard from Streamer Board & Console.
